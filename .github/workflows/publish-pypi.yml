name: Publish to PyPI

on:
  push:
    tags: ["v*.*.*"]

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Bump versions in pyproject.toml files
        run: |
          VERSION=${{ steps.version.outputs.VERSION }}
          for f in packages/*/pyproject.toml; do
            sed -i "s/^version = \"[^\"]*\"/version = \"${VERSION}\"/" "$f"
          done

      - name: Build all packages
        run: |
          for pkg in packages/trace-agent-sdk packages/trace-agent-server packages/trace-agent-langchain packages/trace-agent-ui; do
            uv build --package $(basename "$pkg")
          done

      - name: Publish to PyPI
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          for pkg in trace-agent-sdk trace-agent-server trace-agent-langchain trace-agent-ui; do
            uv publish dist/${pkg}-${{ steps.version.outputs.VERSION }}-py3-none-any.whl
            uv publish dist/${pkg}-${{ steps.version.outputs.VERSION }}.tar.gz
          done
